# Dependencies
node_modules/
.pnp
.pnp.js

# Environment — NEVER commit these
.env
.env.local
.env.*.local

# Build outputs
dist/
build/
.next/

# Prisma
backend/prisma/migrations/*.sql.bak

# Logs
logs/
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Docker
.docker/

# Test coverage
coverage/

# Source data — Zoho exports, customer PII, price catalogs. NEVER commit.
etl/data/
etl/source/
*.xlsx
*.csv

# ───────────────────────────────────────────────────────────────
# Internal planning, agent & design docs — LOCAL ONLY, never commit.
# This repo is public; these contain business strategy, partner names,
# agent instructions, and design references that must not be published.
# History audited 2026-06-06: none were ever committed (clean) — see
# chore/gitignore-and-account-seed. Previously only untracked, not ignored;
# a `git add .` would have leaked them. Now properly ignored.
# ───────────────────────────────────────────────────────────────
CLAUDE.md
AGENTS.md
STATUS.md
FOUNDATION.md
ProjectOverview.md
BRIEFING_*
memory/
QuoteFeatureContext/
_enrichment_wip/
graphify-out/
redesign/
.agents/
.claude/
ADR/002-domain-layer-and-platform-foundations.md
